Run instructions

1) Open a terminal in the project folder:


2) Build (Windows with GCC / Linux / macOS):

```powershell
make
```

3) Run the program:

```powershell
./main path_to_stopword_file path_to_text_file
```

4) Clean build artifacts:

```powershell
make clean
```

